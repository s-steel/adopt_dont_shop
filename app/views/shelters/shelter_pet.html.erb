<h1>Pets Available For Adoption:</h1>

<% @pets.each do |pet| %>
  <img src=" <%= pet.image %>" style="width:50px;height:50px;">
  <h3><%= pet.name %></h3><br>
  <p>Approximate Age: <%= pet.approximate_age %><br>
  <p>Sex: <%= pet.sex %></p>
<% end %>

<form action="/shelters/<%= @shelter.id %>/pets/new">
  <input type="submit" value="Create Pet">
</form>
